<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('task.title') %>:</strong>
  <%= @task.title %>
</p>

<p>
  <strong><%= t('task.content') %>:</strong>
  <%= @task.content %>
</p>

<%= link_to t('task.Edit'), edit_task_path(@task) %> |
<%= link_to t('task.Back'), tasks_path %>
